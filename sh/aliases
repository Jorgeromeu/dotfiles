# vim:ft=sh

alias sudo='sudo '

# shorteneed commands
alias v='nvim'
alias g='git'
alias t='tmux'
alias pm='pacman'
alias cl='clear'
alias ccat='highlight'

# default options
alias cp='cp -v'
alias mv='mv -v'
alias rm='rm -v'

# alias ls='ls -hN --color=auto --group-directories-first'
# alias ll='ls -l'
# alias la='ls -A'
# alias lla='ls -lA'

alias ls='exa --color=auto --group-directories-first'
alias ll='exa --group-directories-first -l --git'
alias la='exa --color=auto --group-directories-first -a'
alias la='exa --color=auto --group-directories-first -l --git -a'

alias grep="grep --color=auto"
alias gcc="gcc -Wall -g"

# fun
alias starwars="telnet towel.blinkenlights.nl"

